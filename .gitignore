# Session-local agent context: lives on disk, never in the repo.
CLAUDE.md
CLAUDE.local.md

# Plugin build output (rebuilt on demand by script/build-plugin for manual
# upload): the bundled MCP server and the packaged plugin archive.
plugin/mcp-server/
*.plugin
