# Session-local agent context: lives on disk, never in the repo.
CLAUDE.md
CLAUDE.local.md

# Plugin build output (rebuilt on demand by script/build-plugin for manual upload).
*.plugin

# Bundled MCP server, fetched from the pinned climbx-mcp tag by script/build-plugin.
plugin/mcp-server/
