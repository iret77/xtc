# Session-local agent context: lives on disk, never in the repo.
CLAUDE.md
CLAUDE.local.md

# The packaged plugin archive (built on demand for manual upload). A marketplace
# or git install uses the committed plugin/ directory instead, so plugin/mcp-server/
# IS committed to keep ./plugin self-contained; rebuild it with script/build-plugin
# after changing anything under mcp/.
*.plugin
