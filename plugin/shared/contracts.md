# Runtime contracts

The runtime copy of the shared data shapes, storage paths, and protocols the skills depend on
(opportunity object, seen store, config, snapshots, ranking formula, handoff strings).

Scaffold placeholder. The relevant subset of `docs/plugin/contracts.md` is copied in here in a
later issue so the plugin carries its contracts at runtime.
