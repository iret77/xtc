# Guardrails

The write-confirmation protocol, daily-cap rules, and draft guards that every ClimbX skill
shares as its single source of truth. No skill restates these rules in its own words.

Scaffold placeholder. The concrete rules are ported here from `docs/plugin/contracts.md`
sections 8 (write-confirmation protocol) and 9 (draft guards) in a later issue.
