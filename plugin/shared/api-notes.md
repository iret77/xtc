# API notes

A cheat sheet for the bundled ClimbX MCP tools: what each returns, the rate budget, and the
user-facing error playbook (missing key, read-only key, cap reached, rate limited, and so on).

Scaffold placeholder. The tool list and error playbook are filled in from
`docs/plugin/contracts.md` sections 10 and 11 in a later issue.
