---
description: ClimbX companion for X growth (scan, draft, ship, engage, analytics)
disable-model-invocation: true
argument-hint: "[what you want to do]"
---

Open the ClimbX companion for X (Twitter) growth.

Invoke the `climbx` skill from the climbx-cowork plugin and follow it. It routes to the right
workflow: scan the niche for outliers, draft a post or reply in the account's voice, schedule or
publish, or open the dashboard.

The user's request: $ARGUMENTS

If that request is empty, briefly show what ClimbX can do (scan, draft, ship, engage, dashboard) and
ask what they want to start with. If no API key is configured yet, run `/climbx-setup` first.
