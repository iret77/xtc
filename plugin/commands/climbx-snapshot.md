---
description: Back up and diff your ClimbX voice profile and learnings
disable-model-invocation: true
argument-hint: "[backup | diff, optional]"
---

Back up or diff the ClimbX voice profile and learnings the account owns.

Invoke the `climbx-snapshot` skill from the climbx-cowork plugin and follow it: keep timestamped
local JSON copies under `~/.climbx/snapshots/` and explain what changed in plain language.

The user's request: $ARGUMENTS
