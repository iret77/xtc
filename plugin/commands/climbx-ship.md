---
description: Publish or schedule an approved post through ClimbX, and manage the queue
disable-model-invocation: true
argument-hint: "[publish now | schedule <when> | show/reschedule/cancel queue]"
---

Publish or schedule an approved X (Twitter) post through ClimbX, or manage the scheduled queue.

Invoke the `climbx-ship` skill from the climbx-cowork plugin and follow it: always show the exact
final text and the remaining daily cap before writing, and write only after an explicit
confirmation. It also lists, reschedules, and cancels the queue.

The user's shipping request: $ARGUMENTS
