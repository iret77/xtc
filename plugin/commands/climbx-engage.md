---
description: Draft a reply to someone else's X post in your voice, ready to copy
disable-model-invocation: true
argument-hint: "[the post you want to reply to]"
---

Draft a reply to someone else's X (Twitter) post in the account owner's voice.

Invoke the `climbx-engage` skill from the climbx-cowork plugin and follow it: use ClimbX
`suggest_reply`, explain the credit and lock states in plain language, and return a reply the user
posts by hand (the API does not publish replies).

The post to reply to: $ARGUMENTS
